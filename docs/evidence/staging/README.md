# Staging evidence directory

Only redacted, reviewable evidence belongs here. Expected generated files include `database-security.json`, provider metadata summaries without credentials, restore invariant counts, and the finalized staging acceptance report.

Never place database dumps, PostgreSQL URLs, OAuth credentials, session Cookies, environment exports, Playwright storage state/traces, raw HAR files, or unredacted logs in this directory.
